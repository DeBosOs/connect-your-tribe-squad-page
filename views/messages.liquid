{% for message in messages %}

    <p>{{ message }}</p>

{%endfor%}

<form method="post" action="/berichten">
    <label>Tekstje:
        <input type="tekst" name="tekstje" required>
    </label>
</form>
