{% render 'partials/head.liquid', title: 'Squad page' %}

<ul class="dropdown-menu">
  <li>
    <button class="dropdown-btn">Leeftijd👇</button>
    <div class="dropdown-content">
      <a href="?sort=16-17">16-17 jaar</a>
      <a href="?sort=18-19">18-19 jaar</a>
      <a href="?sort=20-21">20-21 jaar</a>
    </div>
  </li>
  <li>
    <button class="dropdown-btn">Favoriete kleur👇</button>
    <div class="dropdown-content">
      <a href="?sort=blue">Blauw</a>
      <a href="?sort=red">Rood</a>
      <a href="?sort=green">Groen</a>
    </div>
  </li>
  <li>
    <button class="dropdown-btn">Favoriete HTML-tag👇</button>
    <div class="dropdown-content">
      <a href="?sort=div">div</a>
      <a href="?sort=p">p</a>
      <a href="?sort=button">button</a>
    </div>
  </li>
</ul>

<div class="container">
  {% for person in persons %}
    <section>
      {% if person.avatar == null %}
        <img src="images/default_pfp.jpg" alt="">
      
      {%  elsif person.avatar == "" %}
        <img src="images/default_pfp.jpg" alt="">
    
      {% else %}
        <img src="{{ person.avatar }}" alt="">
      {% endif  %}
        
      <p>
        {{ person.name }}
      </p>
      <p>

        {% assign birth_timestamp = person.birthdate | date: '%s' | default: 0 %}
        {% assign now_timestamp = 'now' | date: '%s' %}
        {% assign age = now_timestamp | minus: birth_timestamp | divided_by: 31556952 | floor %}
        {{ age }}

        </p>
    </section>

    {% endfor %}
    
    </div>

    {% render 'partials/foot.liquid', squads: squads %}